// ------------------------------------------------------------
// Most of the functions inside `method` are
// functions to safe wrap and translate types from `utils` functions.
//
// The goal is to keep `method` return types as simple as possible before passing it to flutter dart type.
// ------------------------------------------------------------

pub mod init_torrent_session;
pub mod get_torrent_info;
pub mod spawn_stream_server;
pub mod generate_torrent_handle;
